#include <bits/stdc++.h>
using namespace std;

int main(){
    long long t;
    cin >> t;

    while(t--);
    long long len;
    cin >> len;

    vector arr(n);
    for (int i = 0; i < len; i++){
        arr.push_back(i);
    }

    

}